{
    "name": "backend",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "dev": "nodemon server.js",
        "deploy": "rm -rf dist && cd ./frontend/ && npm run build && cp -r build ../dist && cd .. && fly deploy",
        "build": "rm -rf dist && cd ./frontend/ && npm run build && cp -r build ../dist && cd .."
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "body-parser": "^1.20.2",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "mongodb": "^6.1.0"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}
